import styled from "styled-components";

const Container = styled.div`
    
`

const Card: React.FC = () => {
    return (
        <Container>
            Card
        </Container>
    )
}

export default Card;